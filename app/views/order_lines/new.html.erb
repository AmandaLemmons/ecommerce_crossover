<%= @product.description %>
<%= @product.name %>
<%= @product.price %>



  <%= simple_form_for(@orderline) do |f| %>
    <%=f.input :qty, label: false, placeholder:   "Quanty" %>
    <%=f.hidden_field :product_id, :value => @product.id %>

    <%=f.submit "Order", class: "btn btn-primary btn-large form-btn"%>

  <% end %>
